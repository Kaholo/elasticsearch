{
	"name": "kaholo-plugin-elasticsearch",
	"version": "2.0.0",
	"description": "Kaholo plugin for integration with Elasticsearch .",
	"main": "app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Kaholo Group(Amit Alon)",
	"license": "ISC",
	"dependencies": {
		"@elastic/elasticsearch": "^7.16.0"
	}
}
